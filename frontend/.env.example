# Secrets consumed by app.config.js at build time (Node/Expo CLI only —
# never referenced from config.yaml or from screens/ code).
# Copy this file to .env and fill in real values; .env is gitignored.
